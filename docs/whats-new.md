# Что нового

## 2026-06-23 - доступный часовой пояс расписания

Продолжен backlog roadmap по accessibility-полировке редактора рассылок:

- select часового пояса в `ScheduleBuilder` получил явную `aria-label`;
- компонентный тест `ScheduleBuilder` закрепляет доступную подпись поля вместе с отображением проектного часового пояса;
- поведение расчета расписания не менялось, изменена только доступная разметка выбора часового пояса.

Проверки этого этапа включают targeted frontend-тест `ScheduleBuilder`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-23 - доступные фильтры задач

Продолжен backlog roadmap по accessibility-полировке рабочих экранов:

- select-фильтры задач по организации и сотруднику получили явные `aria-label`;
- компонентный тест `WorkPage` закрепляет доступные подписи фильтров вместе с поиском и машинно-читаемыми датами задач;
- поведение фильтрации задач не менялось, изменена только доступная разметка панели фильтров.

Проверки этого этапа включают targeted frontend-тест `WorkPage`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-23 - доступный поиск организаций для рассылки

Продолжен backlog roadmap по accessibility-полировке рассылок:

- поиск в модальном справочнике выбора организаций для рассылки получил явную `aria-label` через общий `SearchPanel`;
- компонентный тест `OrganizationPicker` закрепляет доступную подпись поиска вместе с черновиком выбора получателей;
- поведение фильтрации и выбора организаций не менялось, изменена только доступная разметка поля поиска.

Проверки этого этапа включают targeted frontend-тест `OrganizationPicker`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-23 - доступный поиск задач

Продолжен backlog roadmap по accessibility-полировке рабочих экранов:

- поиск на странице работы получил явную `aria-label` через общий `SearchPanel`;
- компонентный тест `WorkPage` теперь закрепляет доступную подпись поиска вместе с машинно-читаемыми датами задач;
- поведение фильтрации задач не менялось, изменена только доступная разметка поля поиска.

Проверки этого этапа включают targeted frontend-тест `WorkPage`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-23 - доступный поиск SMTP-профилей

Продолжен backlog roadmap по accessibility-полировке настроек рассылок:

- поиск в списке SMTP-профилей получил явную `aria-label` через общий `SearchPanel`;
- компонентный тест `TransportProfilesPage` закрепляет доступную подпись поиска вместе с вкладками настроек;
- поведение фильтрации SMTP-профилей не менялось, изменена только доступная разметка поля поиска.

Проверки этого этапа включают targeted frontend-тест `TransportProfilesPage`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-23 - доступный поиск организаций

Продолжен backlog roadmap по accessibility-полировке справочников:

- основной поиск в списке организаций получил явную `aria-label` через общий `SearchPanel`;
- компонентный тест `OrganizationsPage` теперь закрепляет доступную подпись общего поиска вместе с поиском района;
- поведение фильтрации организаций не менялось, изменена только доступная разметка поля поиска.

Проверки этого этапа включают targeted frontend-тест `OrganizationsPage`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступный поиск сотрудников

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев справочников:

- поиск в списке сотрудников получил явную `aria-label` через общий `SearchPanel`;
- добавлен компонентный тест `EmployeesPage`, который закрепляет подпись поиска и загрузку списка сотрудников через mock API;
- страница сотрудников теперь покрыта отдельным frontend-тестом на базовую доступность поиска.

Проверки этого этапа включают targeted frontend-тест `EmployeesPage`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные фильтры списка рассылок

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев рассылок:

- поиск в списке рассылок получил явную `aria-label` через общий `SearchPanel`;
- select фильтра статуса рассылок получил доступное имя;
- компонентный тест `CampaignsPage` закрепляет подписи поиска, статусного select и quick-фильтров.

Проверки этого этапа включают targeted frontend-тест `CampaignsPage`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступный поиск по очереди рассылок

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев рассылок:

- общий `SearchPanel` получил опциональную подпись поискового поля через `inputAriaLabel`;
- поиск на странице очереди рассылок теперь явно озвучивает, что ищет по email, организации, ошибке и SMTP-ответу;
- компонентный тест `DispatchPage` закрепляет доступную подпись общего поиска вместе с остальными фильтрами очереди.

Проверки этого этапа включают targeted frontend-тесты `DispatchPage` и `SearchPanel`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - контекстные действия организаций

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев организаций:

- desktop context menu строки организации получил доступное имя с названием выбранной организации;
- пункты `Создать`, `Редактировать`, `Обновить` и `Удалить` теперь имеют контекстные `aria-label`;
- компонентный тест `OrganizationsPage` открывает меню через `contextmenu` и закрепляет доступные подписи действий.

Проверки этого этапа включают targeted frontend-тест `OrganizationsPage`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступный поиск района в списке организаций

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев организаций:

- поле поиска района в боковой панели списка организаций получило явную `aria-label`;
- новый компонентный тест `OrganizationsPage` закрепляет доступную подпись поиска, status-область активных фильтров и передачу выбранного района в API;
- фильтр районов теперь покрыт на уровне страницы, а не только через общие helper-тесты.

Проверки этого этапа включают targeted frontend-тест `OrganizationsPage`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступное состояние фильтра районов в справочнике

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев рассылок:

- кнопки районов в модалке `OrganizationPicker` получили `aria-pressed`;
- выбранность фильтра `Все` и конкретного района теперь доступна не только визуально через `active`;
- компонентный тест открывает справочник через mock API и закрепляет переключение доступного состояния фильтра районов.

Проверки этого этапа включают targeted frontend-тест `OrganizationPicker`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные мобильные действия выбора организаций

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев рассылок:

- мобильные кнопки `Выбрать`/`Выбрано` в справочнике `OrganizationPicker` получили контекстные `aria-label` с названием организации;
- подписи мобильных действий теперь совпадают по смыслу с desktop checkbox и различают добавление и удаление из черновика получателей;
- компонентный тест закрепляет наличие доступной кнопки мобильной карточки вместе с проверкой счетчика выбранных организаций.

Проверки этого этапа включают targeted frontend-тест `OrganizationPicker`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступный счетчик выбранных организаций

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев рассылок:

- счетчик `Выбрано` в модалке `OrganizationPicker` размечен как live status область;
- `aria-label` счетчика теперь явно сообщает количество организаций в черновике получателей;
- компонентный тест закрепляет начальное значение счетчика и его обновление после выбора организации в справочнике.

Проверки этого этапа включают targeted frontend-тест `OrganizationPicker`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные чекбоксы справочника организаций

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев рассылок:

- чекбоксы выбора организаций в модалке `OrganizationPicker` получили контекстные `aria-label` с названием организации;
- подпись чекбокса теперь различает действие добавления в черновик получателей и удаления из него;
- компонентный тест открывает справочник через mock API и закрепляет доступные имена для уже выбранной и новой организации.

Проверки этого этапа включают targeted frontend-тест `OrganizationPicker`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - предупреждение об организациях без email

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев рассылок:

- расчет известных email у выбранной организации вынесен в общий helper `getOrganizationKnownEmailCount`;
- `OrganizationPicker` показывает доступное предупреждение, если среди выбранных получателей есть организации без email;
- карточки выбранных организаций без адреса получили компактный бейдж `Нет email`, а тесты закрепляют helper и UI-разметку.

Проверки этого этапа включают targeted frontend-тесты `campaignRecipients` и `OrganizationPicker`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступный список выбранных организаций

Продолжен backlog roadmap по accessibility-полировке менее частых сценариев рассылок:

- сводка выбранных организаций в `OrganizationPicker` размечена как доступный список метрик;
- список организаций, выбранных для рассылки, получил явный `role="list"` и элементы `role="listitem"`, а пустое состояние оставлено вне списка;
- кнопка удаления организации из получателей получила контекстный `aria-label`, а компонентный тест закрепляет новую разметку.

Проверки этого этапа включают targeted frontend-тест `OrganizationPicker`, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - сводка фильтров организаций

Продолжен backlog roadmap по accessibility-полировке и performance-проверкам рабочих списков:

- состояние фильтров списка организаций теперь собирается через общий `buildOrganizationListFilterSummary`;
- счетчик активных фильтров, подпись кнопки и текст summary формируются консистентно в одном helper-е;
- summary активных фильтров в UI размечена как доступная status-область, а helper покрыт unit-тестом.

Проверки этого этапа включают targeted frontend-тест фильтров организаций, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - быстрый список проблем кампании

Продолжен backlog roadmap по performance-проверкам для больших списков кампаний и dispatch items:

- статистика кампании теперь собирает последнюю проблемную отправку и ограниченный список проблем через общий `buildCampaignProblemSummary`;
- `CampaignStatsPanel` больше не запускает два отдельных расчета для одного набора failed/deferred сообщений;
- unit-тест закрепляет порядок top-N проблем, latest item, нулевой лимит и неизменность исходных массивов.

Проверки этого этапа включают targeted frontend-тесты статистики кампании, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - быстрый расчет сводки очереди

Продолжен backlog roadmap по performance-проверкам для больших списков dispatch items:

- сводка страницы очереди и последняя проблемная отправка теперь считаются одним линейным helper-ом без повторных проходов и сортировки;
- `DispatchPage` использует общий `buildDispatchItemsSummary`, чтобы не дублировать вычисления в UI;
- unit-тест закрепляет счетчики, выбор последней проблемы и сохранение исходного порядка списка.

Проверки этого этапа включают targeted frontend-тесты диагностики очереди и страницы очереди, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - сводка последней проблемы очереди

Продолжен backlog roadmap по наблюдаемости очереди рассылок и SMTP-ошибок:

- на странице очереди добавлен статусный блок последней проблемной отправки на текущей странице;
- блок показывает получателя, статус, количество попыток, время ошибки, следующую попытку и SMTP-ответ, если он есть;
- helper `findLatestDispatchProblemItem` и `DispatchPage` покрыты unit-тестами.

Проверки этого этапа включают targeted frontend-тесты диагностики очереди и страницы очереди, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные фильтры очереди рассылок

Продолжен backlog roadmap по accessibility-полировке менее частых рабочих сценариев:

- сводка очереди рассылок теперь размечена как доступный список метрик для текущей страницы;
- фильтры очереди по статусу, ID кампании и ID партии получили явные `aria-label`;
- unit-тест `DispatchPage` закрепляет доступную разметку summary и фильтров.

Проверки этого этапа включают targeted frontend-тест страницы очереди, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - fallback проверки БД на LegacyDb

Укреплена post-deploy проверка SQL Server в `scripts/check-mailing-db.ps1`:

- если `ConnectionStrings:MailingDb` в production config содержит только XPO provider token без SQL-настроек, скрипт использует `ConnectionStrings:LegacyDb`;
- fallback теперь проверяет наличие SQL Server source и initial catalog, поэтому служебные параметры в `MailingDb` не блокируют переход на `LegacyDb`;
- post-deploy проверка также умеет брать connection string из переменных окружения, опубликованного `web.config` и IIS app pool config без вывода секретов в лог;
- fallback соответствует текущей архитектуре, где legacy и mailing контуры работают с одной основной SQL Server БД;
- при отсутствии SQL-настроек в обеих строках скрипт теперь падает с явным сообщением вместо пустой строки подключения.

Проверки этого этапа включают targeted script-проверку fallback на временном config, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки, локальную DB/E2E-проверку рассылки против SQL-БД и повторную проверку GitHub Actions после push.

## 2026-06-22 - проверка БД понимает XPO provider token

Исправлена production-проверка `scripts/check-mailing-db.ps1` после GitHub Actions deploy:

- SQL Server connection string теперь корректно извлекается не только из `XpoProvider=MSSqlServer;...`, но и из XPO-формата `MSSqlServer;...`;
- первый provider token без `=` отбрасывается перед передачей строки в `SqlConnectionStringBuilder`;
- проверка БД после deploy больше не падает на production config с XPO provider token.

Проверки этого этапа включают targeted script-проверку `check-mailing-db.ps1` на XPO-строках, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки, локальную DB/E2E-проверку рассылки против SQL-БД и повторную проверку GitHub Actions после push.

## 2026-06-22 - стабильный timezone fallback расписания

Исправлена нестабильность backend-проверки расписания на Windows Server 2016 runner:

- для стандартного `Asia/Novosibirsk` добавлен fallback на Windows time zone `N. Central Asia Standard Time`;
- preview расписания больше не откатывается в UTC на runner, где IANA-идентификатор недоступен;
- существующий backend-тест расписания закрепляет ожидаемое локальное время по умолчанию.

Проверки этого этапа включают targeted backend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки, локальную DB/E2E-проверку рассылки против SQL-БД и повторную проверку GitHub Actions после push.

## 2026-06-22 - единый актуальный roadmap

Roadmap-документация приведена к одному актуальному файлу `docs/roadmap.md`:

- старый подробный `docs/project-roadmap.md` и промежуточный `docs/roadmap-status.md` удалены;
- новый roadmap объединяет выполненные блоки, формально незакрытые пункты и ближайший backlog;
- индекс документации теперь ссылается только на канонический roadmap.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки, локальную DB/E2E-проверку рассылки против SQL-БД и проверку GitHub Actions после push.

## 2026-06-22 - сводка статуса roadmap

Добавлен документ `docs/roadmap.md`, который отвечает на вопрос о закрытии roadmap и отделяет реализованный MVP-контур от формальной приемки:

- зафиксировано, что основные P0/P1-инкременты по SQL Server, рассылкам, организациям, адаптивности, тестам и IIS/autodeploy закрыты для MVP;
- отдельно перечислены частично закрытые зоны: финальный ручной smoke, production/IIS подтверждение и решение по P2/P3;
- описаны шаги, после которых roadmap можно считать формально закрытым.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные типы расписания кампании

Продолжен инкремент roadmap 4.8/7.3 по доведению расписания рассылок до доступного рабочего вида:

- переключатель типа расписания получил `aria-label` с названием режима и состоянием выбора;
- видимый текст вариантов, активный класс, подсказки и контролируемое переключение режима сохранены без изменения;
- unit-тест `ScheduleBuilder` закрепляет `aria-selected`, новые доступные подписи и обновление выбранного типа после клика.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные вкладки редактора кампании

Продолжен инкремент roadmap 4.8/7.3 по доведению редактора рассылок до доступного рабочего вида:

- вкладки разделов редактора кампании получили `aria-label` с названием раздела и состоянием перехода;
- видимый текст вкладок, активный класс и переключение между разделами сохранены без изменения;
- Playwright smoke-тест редактора закрепляет доступные подписи вкладок для новой кампании и вкладки статистики существующей кампании.

Проверки этого этапа включают targeted Playwright-тест редактора кампании, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные фильтры сообщений кампании

Продолжен инкремент roadmap 4.8/7.3 по доведению статистики рассылок до доступного рабочего вида:

- фильтры последних сообщений кампании получили `aria-label` с названием статуса и состоянием применения;
- видимый текст фильтров, активный класс и фильтрация списка сообщений сохранены без изменения;
- unit-тест `CampaignStatsPanel` закрепляет `aria-selected`, новые доступные подписи фильтров и переключение на ошибочные сообщения.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные вкладки очереди рассылок

Продолжен инкремент roadmap 4.8/7.3 по доведению очереди рассылок до доступного рабочего вида:

- вкладки сообщений и партий очереди получили `aria-label` с названием раздела и состоянием перехода;
- видимый текст вкладок, активный класс и переключение между сообщениями и партиями сохранены без изменения;
- unit-тест `DispatchPage` закрепляет `aria-selected`, новые доступные подписи вкладок и машинно-читаемые даты таблиц.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные группы настроек SMTP

Продолжен инкремент roadmap 4.8/7.3 по доведению настроек рассылок до доступного рабочего вида:

- вкладки групп настроек SMTP получили `aria-label` с названием раздела и состоянием перехода;
- видимый текст вкладок, активный класс и переключение между группами сохранены без изменения;
- unit-тест `TransportProfilesPage` закрепляет `aria-selected` и новые доступные подписи вкладок настроек.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступный режим просмотра событий организации

Продолжен инкремент roadmap 3.4/7.3 по доведению истории организации до доступного рабочего вида:

- переключатель режима событий организации получил `aria-label` с названием режима и состоянием выбора;
- видимый текст режимов, активный класс и обработка переключения сохранены без изменения;
- unit-тест `OrganizationEventViewModeTabs` закрепляет `aria-selected`, активный класс и новые доступные подписи режимов.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные вкладки снимков 1С организации

Продолжен инкремент roadmap 3.4/7.3 по доведению навигации карточки организации до доступного рабочего вида:

- вкладки снимков 1С получили `aria-label` с названием снимка и состоянием перехода;
- видимый текст снимков, активный класс, пустой контейнер и обработка выбора сохранены без изменения;
- unit-тест `OrganizationSnapshotTabs` закрепляет `aria-selected`, активный класс и новые доступные подписи снимков.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные вкладки истории организации

Продолжен инкремент roadmap 3.4/7.3 по доведению навигации карточки организации до доступного рабочего вида:

- вкладки истории организации получили `aria-label` с названием раздела и состоянием перехода;
- видимый текст вкладок, активный класс и обработка выбора сохранены без изменения;
- unit-тест `OrganizationHistoryTabs` закрепляет `aria-selected`, активный класс и новые доступные подписи вкладок истории.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные разделы карточки организации

Продолжен инкремент roadmap 3.4/7.3 по доведению навигации карточки организации до доступного рабочего вида:

- главные вкладки карточки организации получили `aria-label` с названием раздела и состоянием перехода;
- видимый текст вкладок, активный класс и обработка выбора сохранены без изменения;
- unit-тест `OrganizationViewTabs` закрепляет `aria-selected`, активный класс и новые доступные подписи вкладок.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные табы связанных данных организации

Продолжен инкремент roadmap 3.4/7.3 по доведению карточки организации до доступной навигации по связанным данным:

- inline-табы связанных данных организации получили `aria-label` с названием раздела, количеством записей и состоянием выбора;
- preview-карточки и табы теперь используют общий helper доступной подписи, чтобы одинаково озвучивать один и тот же раздел;
- unit-тест `OrganizationRelationsOverview` закрепляет видимый текст табов, `aria-selected` и новые доступные подписи.

Проверки этого этапа включают targeted frontend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные карточки связанных данных организации

Продолжен инкремент roadmap 3.4/7.3 по доведению карточки организации до устойчивого и доступного рабочего вида:

- preview-карточки связанных данных организации получили явный `aria-label` с названием раздела, количеством записей и текущим состоянием выбора;
- визуальная разметка карточек, формат счетчика и обработка выбора сохранены без изменения;
- unit-тест `RelationPreviewCard` закрепляет доступную подпись для активной и неактивной карточки.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемая дата последнего входа

Продолжен инкремент roadmap 7.3 по доведению входа в систему до доступного и стабильного UI:

- дата последнего успешного входа в карточке быстрого выбора пользователя теперь рендерится через `<time dateTime>`;
- видимый текст карточки, разделители и выбор последнего пользователя сохранены без изменения;
- unit-тест `LoginPage` закрепляет чтение профиля из `puls-last-login-profile` и исходное UTC-значение `dateTime`.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты списка задач

Продолжен инкремент roadmap 3.4/7.3 по доведению рабочих списков до устойчивого и доступного вида:

- даты создания и срока задач теперь рендерятся через `<time dateTime>`;
- скрываемая колонка завершения задач сохраняет исходное UTC-значение в `dateTime` при включении через настройки таблицы;
- unit-тест `WorkPage` закрепляет машинно-читаемые даты для всех date-колонок списка задач.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты списка кампаний

Продолжен инкремент roadmap 4.8/7.3 по доведению UI рассылок до устойчивого и доступного рабочего вида:

- даты следующего и последнего запуска кампаний теперь рендерятся через `<time dateTime>`;
- скрываемые колонки создания и обновления кампаний сохраняют исходные UTC-значения в `dateTime` при включении через настройки таблицы;
- unit-тест `CampaignsPage` закрепляет машинно-читаемые даты для всех date-колонок списка.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты очереди рассылок

Продолжен инкремент roadmap 4.8/7.3 по доведению очереди рассылок до устойчивого и доступного рабочего вида:

- даты последнего события и следующей попытки сообщения теперь рендерятся через `<time dateTime>`;
- плановая, созданная и завершенная даты партий отправки также получили машинно-читаемый `dateTime`;
- unit-тест `DispatchPage` закрепляет исходные UTC-значения для таблицы сообщений и таблицы партий.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты SMTP-профилей

Продолжен инкремент roadmap 4.8/7.3 по доведению настроек рассылок до устойчивого и доступного рабочего вида:

- даты создания и обновления SMTP-профилей в таблице теперь рендерятся через `<time dateTime>`;
- мета-строка модалки редактирования профиля использует тот же форматтер и сохраняет исходные UTC-значения в `dateTime`;
- unit-тест `TransportProfilesPage` закрепляет машинно-читаемые даты в таблице и в модалке редактирования.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты превью расписания

Продолжен инкремент roadmap 4.8/7.3 по доведению UI рассылок до устойчивого и доступного рабочего вида:

- локальное и UTC-время ближайших запусков в превью расписания теперь рендерятся через `<time dateTime>`;
- видимый формат дат, подпись часового пояса и layout превью сохранены без изменения;
- unit-тест `ScheduleBuilder` закрепляет исходные `dateTime`-значения для локального времени и UTC.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты статистики кампании

Продолжен инкремент roadmap 4.8/7.3 по доведению статистики и очереди рассылок до устойчивого рабочего вида:

- даты ошибок и следующих попыток в списке проблем кампании теперь рендерятся через `<time dateTime>`;
- даты последних пакетов и сообщений очереди получили машинно-читаемое `dateTime` в основной таблице и адаптивных карточках;
- unit-тест `CampaignStatsPanel` закрепляет исходные UTC-значения для problem list, batches и recent items.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты быстрых карточек связей

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- даты в preview-карточках документов, договоров, реализаций, лицензий и заказов Парус теперь рендерятся через `<time dateTime>`;
- видимые подписи быстрых карточек, разделители и суммы сохранены без изменения layout;
- unit-тест `OrganizationRelationsSection` закрепляет исходные UTC-значения для всех дат preview-карточек.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые периоды сопровождения

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- периоды лицензий 1С и сайта в сводке сопровождения теперь рендерятся через `<time dateTime>`;
- те же машинно-читаемые периоды сохранены в detail-статусах блоков `1С` и `Сайт` без изменения видимого текста;
- unit-тест `OrganizationSupportSection` закрепляет исходные UTC-значения периода в summary и detail-разметке.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты документов организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- date-only колонки документов, договоров и реализаций организации теперь рендерятся через `<time dateTime>`;
- видимый формат дат, денежные значения, статусы и fallback-значения таблиц сохранены;
- unit-тест `OrganizationRelationsWorkspace` закрепляет исходные UTC-значения для документов, договоров и реализаций.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты таблиц Парус

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- даты выдачи и окончания лицензий Парус в табличном режиме теперь рендерятся через `<time dateTime>`;
- дата заказа Парус также сохраняет исходное UTC-значение в `dateTime` без изменения видимого текста;
- unit-тест `OrganizationRelationsWorkspace` закрепляет DOM-разметку для лицензий и заказов Парус.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты таблицы истории организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- даты события и периода в табличном режиме истории организации теперь рендерятся через `<time dateTime>`;
- видимый текст таблицы, timeline-режим и fallback-значения сохранены;
- unit-тест `OrganizationHistoryWorkspace` закрепляет исходные UTC-значения для всех трех date-колонок события.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемая дата паспорта организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- короткая дата обновления в заголовке `Паспорт организации` теперь рендерится через `<time dateTime>`;
- видимое значение даты и layout сайдбара сохранены;
- unit-тест `OrganizationSidebar` закрепляет исходное UTC-значение для короткой даты и audit-строки.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемая дата изменения в сайдбаре организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- дата последнего изменения в сайдбаре карточки организации теперь рендерится через `<time dateTime>`;
- видимый текст audit-строки с автором и fallback-значение сохранены без изменения layout;
- unit-тест `OrganizationSidebar` закрепляет исходное UTC-значение даты изменения.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемая дата соглашения ПФР

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- дата соглашения с ПФР в блоке `ЭЦП и банковские реквизиты` теперь рендерится через `<time dateTime>`;
- видимый текст, банковские реквизиты, комментарии ЭЦП и fallback-значения сохранены без изменения layout;
- unit-тест `OrganizationBankDetails` закрепляет исходное UTC-значение даты соглашения.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты зарплатного сопровождения

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- даты начала и окончания работы в блоке `Зарплата` теперь рендерятся через `<time dateTime>`;
- видимый текст, контактные ссылки, счетчики и fallback-значения блока сохранены без изменения layout;
- unit-тест `OrganizationSalaryDetails` закрепляет исходные UTC-значения для обеих дат периода работы.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты ИТС организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- даты начала и окончания ИТС в блоке `1С` теперь рендерятся через `<time dateTime>`;
- видимый текст, статус ИТС, ссылки и fallback-значения блока сохранены без изменения layout;
- unit-тест `OrganizationOneCDetails` закрепляет исходные UTC-значения для обеих дат ИТС.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты сайта организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- даты создания сайта и периода лицензии теперь рендерятся через `<time dateTime>`;
- видимый текст и layout блока `Сайт` сохранены без изменения поведения ссылок, статуса и fallback-значений;
- unit-тест `OrganizationSiteDetails` закрепляет исходные UTC-значения для всех трех дат сайта.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты ленты событий организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- дата события в timeline теперь рендерится через `<time dateTime>`;
- даты начала и окончания периода лицензии в событии также получили машинно-читаемую разметку;
- unit-тест `OrganizationEventTimeline` закрепляет исходные UTC-значения для даты события и периода без изменения видимого текста.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты блоков программ

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- дата обновления в карточках блоков программ теперь рендерится через `<time dateTime>`;
- видимый текст `Обновлено` и автор обновления сохранены без изменения layout;
- unit-тест `OrganizationProgramBlocks` закрепляет исходное UTC-значение в `dateTime` вместе с прежними проверками типовых и дополнительных блоков.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - машинно-читаемые даты аудита организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- даты создания, обновления и админского обновления в audit-сводке теперь рендерятся через `<time dateTime>`;
- видимый текст аудита и авторы операций сохранены без изменения layout;
- unit-тест `OrganizationAuditSummary` закрепляет исходные UTC-значения в `dateTime` вместе с прежними проверками списков и счетчиков.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные статусы лицензий организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- статус лицензии в блоке `1С` получил `role=status`;
- статус домена/лицензии в блоке `Сайт` получил такую же статусную семантику;
- unit-тесты `OrganizationOneCDetails` и `OrganizationSiteDetails` закрепляют роль status вместе с прежними проверками ссылок, списков и fallback-значений.

Проверки этого этапа включают targeted frontend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-22 - доступные статусы сводки сопровождения

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- статусные pill в сводке сопровождения организации получили `role=status`;
- прежние tone-классы `ok`, `warn` и `muted` сохранены без изменения layout;
- unit-тест `OrganizationSupportSummary` закрепляет статусную роль для всех визуальных статусов сводки.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные пустые состояния снимков 1С

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- empty-state раздела снимков 1С получил `role=status`, когда по организации нет снимков;
- empty-state выбранного снимка без реквизитов также объявляется как статусное сообщение;
- unit-тесты `OrganizationHistoryWorkspace` и `OrganizationSnapshotDetails` закрепляют статусную роль вместе с прежними проверками текста и detail-grid.

Проверки этого этапа включают targeted frontend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступная пустая история событий организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- empty-state пустой ленты событий организации получил `role=status`;
- текст и layout состояния без событий сохранены, но сообщение теперь объявляется как статусное;
- unit-тест `OrganizationEventTimeline` закрепляет роль status для пустой ленты вместе с прежними проверками событий и лицензионных признаков.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные состояния несохраненной организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- inline empty-state для связанных данных и истории несохраненной организации получили `role=status`;
- тексты и layout предупреждений сохранены, но смена раздела теперь объявляется как статусное сообщение;
- unit-тест `OrganizationRecordMain` закрепляет `role=status` для обоих сценариев несохраненной карточки.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные быстрые переходы организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- быстрые переходы в sidebar карточки организации получили `toolbar` семантику и подпись `aria-label`;
- кнопки переходов сохранили обычную button-семантику и прежние обработчики открытия связанных данных и истории;
- unit-тест `OrganizationSidebar` закрепляет подпись toolbar и количество быстрых команд.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступный контекст sidebar организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- блок контекста карточки в sidebar получил `list`/`listitem` семантику и подпись `aria-label`;
- полное наименование, комментарий и прочая информация остаются в прежнем компактном layout, но теперь объявляются как единая группа;
- unit-тест `OrganizationSidebar` закрепляет порядок доступных групп sidebar и количество элементов в контекстном блоке.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступная audit-сводка организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- audit-сводка карточки организации получила `list`/`listitem` семантику и подпись `aria-label`;
- счетчики связанных данных остались отдельной доступной группой, поэтому даты аудита и счетчики больше не смешиваются в одном наборе;
- unit-тест `OrganizationAuditSummary` закрепляет подписи обеих групп и количество элементов в audit-сводке и счетчиках.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные лицензионные признаки событий организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- лицензионные чипы внутри события организации получили `list`/`listitem` семантику и подпись `aria-label`;
- пустая строка чипов больше не рендерится для событий без ключа и суммы лицензии;
- unit-тест `OrganizationEventTimeline` закрепляет основную ленту событий, вложенный список лицензионных признаков и отсутствие пустого chip-row.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные группы sidebar организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- паспортные данные, статус сопровождения и финансовые показатели в sidebar получили `list`/`listitem` семантику и отдельные подписи `aria-label`;
- прежние контактные ссылки, быстрые действия, email-чипы и задачи сохранены без изменения визуального layout;
- unit-тест `OrganizationSidebar` закрепляет порядок доступных групп и количество элементов в каждой группе.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные поля снимка 1С организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- поля выбранного снимка 1С получили `list`/`listitem` семантику и подпись `aria-label`;
- empty-state снимка без данных сохранен без лишних ролей, поэтому экран не объявляет пустую сетку как список;
- unit-тест `OrganizationSnapshotDetails` закрепляет подпись списка, количество полей, кликабельный телефон и прежние fallback-состояния.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступный блок 1С организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- параметры 1С организации получили `list`/`listitem` семантику и подпись `aria-label`;
- комментарии, доработки и состав ИТС выделены в отдельную доступную группу без изменения прежнего layout;
- unit-тест `OrganizationOneCDetails` закрепляет подписи групп, количество элементов, контактные ссылки и статус ИТС.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступный блок сайта организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- параметры сайта организации получили `list`/`listitem` семантику и подпись `aria-label`;
- комментарий по сайту выделен в отдельную доступную группу без изменения прежнего layout;
- unit-тест `OrganizationSiteDetails` закрепляет подписи групп, количество элементов, нормализацию ссылок и статус домена.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступное зарплатное сопровождение организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- блок зарплатного сопровождения получил `list`/`listitem` семантику для основной сетки показателей;
- состав лицензии и комментарий ЗП выделены в отдельную доступную группу с понятной подписью;
- unit-тест `OrganizationSalaryDetails` закрепляет подписи групп, количество элементов, контактные ссылки и fallback-состояния.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные реквизиты руководителя организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- сетка данных руководителя организации получила `list`/`listitem` семантику и подпись `aria-label`;
- телефон и email руководителя сохраняют прежние `tel:`/`mailto:` ссылки, но теперь находятся внутри доступной группы реквизитов;
- unit-тест `OrganizationDirectorDetails` закрепляет подпись списка и количество элементов вместе с прежними проверками контактных ссылок и fallback-состояний.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные банковские реквизиты организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- сетка банковских реквизитов организации получила `list`/`listitem` семантику и подпись `aria-label`;
- комментарии ЭЦП выделены в отдельную доступную группу, сохраняя прежний компактный layout;
- unit-тест `OrganizationBankDetails` закрепляет подписи групп и количество элементов вместе с прежними проверками значений и fallback-состояний.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные legacy-заметки организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- блок legacy-заметок организации получил `list`/`listitem` семантику и отдельную подпись `aria-label`;
- строки дополнительного комментария, техники и закупок остаются в прежнем компактном layout, но теперь корректно объявляются как элементы одной группы;
- unit-тест `OrganizationLegacyNotes` закрепляет доступную структуру списка вместе с прежними проверками значений и fallback-состояний.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступные списки sidebar организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- почтовые адреса в боковой панели организации получили `list`/`listitem` семантику и подпись `aria-label`;
- чипы связанных задач организации также объявлены как отдельный список, сохраняя прежний компактный layout;
- unit-тест `OrganizationSidebar` закрепляет подписи списков, количество email-адресов и задач вместе с прежними проверками ссылок и быстрых переходов.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступная статусная панель организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- чипы статусной панели организации получили `list`/`listitem` семантику и подпись `aria-label`;
- индикатор сохранения карточки теперь объявлен как `role=status`, сохраняя прежние ok/warn состояния;
- unit-тест `OrganizationStatusBar` закрепляет роли, количество чипов для полной и минимальной панели и состояние сохранения.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - preview-списки связанных данных организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации и связанных данных:

- preview-записи внутри карточек связанных данных получили `list`/`listitem` семантику и подпись `aria-label`;
- сама preview-карточка остается кнопкой с `aria-pressed`, поэтому сценарий выбора раздела не изменился;
- unit-тест `RelationPreviewCard` закрепляет доступную структуру списка, кликабельность карточки и empty-состояние без списка.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - списки блоков программ организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации и раздела сопровождения:

- типовые и дополнительные блоки программ получили отдельные `list`/`listitem` группы с понятными `aria-label`;
- дополнительная группа появляется только для нестандартных вариантов, а пустая карточка сохраняет один список типовых блоков;
- unit-тест `OrganizationProgramBlocks` закрепляет подписи списков, количество карточек и empty-состояние типовых блоков.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступная сводка сопровождения организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до единой доступной структуры:

- summary-карточки сопровождения организации получили явную `list`/`listitem` семантику и `aria-label`;
- пустая сводка сохраняет роль списка без элементов, поэтому состояние остается корректным для assistive UI;
- unit-тест `OrganizationSupportSummary` закрепляет роли, подпись группы, количество карточек и status tone классы.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - счетчики аудита организации

Продолжен инкремент roadmap 3.4/3.5 по доведению карточки организации до понятной рабочей формы:

- сетка счетчиков связанных данных в истории организации получила явную `list`/`listitem` семантику и `aria-label`;
- unit-тест `OrganizationAuditSummary` закрепляет доступную структуру счетчиков вместе с прежними проверками значений;
- визуальный layout карточек аудита не менялся.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-19 - доступная лента событий организации

Продолжен инкремент roadmap 3.4/7.3 по доведению карточки организации и стабильности frontend-проверок:

- лента событий организации получила явную `list`/`listitem` семантику и понятную `aria-label`, не меняя визуальный layout карточек;
- unit-тест `OrganizationEventTimeline` закрепляет доступную структуру списка вместе с прежней проверкой содержимого событий;
- lockfile frontend обновлен для транзитивных `form-data` и `undici`, чтобы `npm audit --audit-level=moderate` снова проходил без уязвимостей.

Проверки этого этапа включают targeted frontend-тест, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - фильтры сообщений статистики кампании

Продолжен инкремент roadmap 4.2/7.3 по доведению карточки кампании и controls рассылок:

- unit-тест `CampaignStatsPanel` закрепляет, что фильтры последних сообщений являются `tablist`/`tab`;
- проверяется начальное активное состояние фильтра `Все` и переключение `aria-selected` на фильтр `Ошибки`;
- сценарий фильтрации последних сообщений по ошибкам остается покрыт вместе с доступным состоянием вкладок.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - вкладки настроек SMTP-профилей

Продолжен инкремент roadmap 4.1/7.3 по доведению разделов рассылок и настроек до единого UX:

- переключатель групп настроек `Основные`/`SMTP профили` получил `tablist`/`tab` семантику и `aria-selected`;
- активное состояние SMTP-группы и скрытие действия создания профиля при переходе в общие настройки сохранены без изменения API-логики;
- unit-тест `TransportProfilesPage` закрепляет доступные роли, подпись группы и переключение активной вкладки.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - режим просмотра событий организации

Продолжен инкремент roadmap 3.4 по адаптивной карточке организации:

- переключатель `Лента`/`Таблица` в истории событий получил `tablist`/`tab` семантику и `aria-selected`;
- визуальное переключение режима сохранено без изменения логики истории событий;
- unit-тест `OrganizationEventViewModeTabs` закрепляет подпись группы, роли, активное состояние и callback выбора режима.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - вкладки снимков 1С организации

Продолжен инкремент roadmap 3.4 по адаптивной карточке организации:

- вложенные вкладки снимков 1С получили `tablist`/`tab` семантику и `aria-selected`;
- пустой контейнер снимков сохраняет подпись группы, поэтому состояние раздела остается понятным даже без архивных данных;
- unit-тест `OrganizationSnapshotTabs` закрепляет роли вкладок, активное состояние, выбор снимка и пустой набор.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - активность preview-карточек связей

Продолжен инкремент roadmap 3.4 по адаптивной карточке организации:

- preview-карточки связанных данных теперь передают активное состояние через `aria-pressed`;
- визуальное выделение карточки и существующий click-сценарий выбора раздела сохранены без изменения бизнес-логики;
- unit-тест `RelationPreviewCard` закрепляет активное и неактивное доступное состояние карточки.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - вкладки связанных данных организации

Продолжен инкремент roadmap 3.4 по адаптивной карточке организации:

- вложенные вкладки связанных данных получили `tablist`/`tab` семантику и `aria-selected`;
- переключатель связей переиспользует общий мобильный scroll-паттерн карточки и не меняет поведение preview-карточек;
- unit-тест `OrganizationRelationsOverview` закрепляет подпись группы, роли вкладок, активное состояние и выбор раздела из карточек и вкладок.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - вложенные вкладки истории организации

Продолжен инкремент roadmap 3.4 по адаптивной карточке организации:

- вкладки истории внутри карточки организации получили отдельную `tablist`/`tab` семантику и `aria-selected`;
- группа истории переиспользует общий мобильный scroll-паттерн карточки, поэтому вложенные вкладки не сжимают содержимое на узких экранах;
- unit-тест `OrganizationHistoryTabs` закрепляет подпись группы, роли вкладок, активное состояние и callback выбора раздела.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - мобильные вкладки карточки организации

Продолжен инкремент roadmap 3.4 по адаптивной карточке организации:

- основные вкладки карточки получили явную `tablist`/`tab` семантику и `aria-selected`, чтобы активный раздел был понятен не только визуально;
- на мобильной ширине вкладки больше не сжимаются и не переносятся в несколько строк, а прокручиваются горизонтальной лентой внутри карточки;
- компактные отступы вкладок закреплены отдельными стилями, не меняя общий вид `settings-tabs` в других разделах;
- unit-тест `OrganizationViewTabs` проверяет ARIA-состояния, активную вкладку и сохранение клика по каждому разделу.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - длинные названия в мобильных карточках

Продолжен инкремент roadmap 3.2 по читаемости таблицы организаций:

- на мобильной ширине шапка карточки DataTable теперь ставит действия под заголовок, чтобы длинное название не конкурировало с меню действий за одну строку;
- действия карточки ограничены шириной контейнера и выравниваются слева в compact layout;
- компонентный тест `OrganizationsTable` и Playwright smoke используют длинное название с непрерывным фрагментом, чтобы закрепить отсутствие горизонтального overflow.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - видимость организации в мобильной карточке

Продолжен инкремент roadmap 3.2 по читаемости таблицы организаций:

- статус `Видимость` теперь показывается в мобильной карточке организации бейджем, как и в desktop-таблице;
- управленческий флаг остается скрытой дополнительной колонкой и не перегружает мобильную карточку;
- компонентный тест `OrganizationsTable` закрепляет отображение статуса в карточке и сохранение скрытого управленческого поля;
- служебная папка `.serena/` добавлена в `.gitignore`, чтобы локальная метаинформация инструмента не попадала в рабочий diff.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - закрытие фильтров организаций из панели

Продолжен инкремент roadmap 3.1 по списку организаций:

- в мобильной/планшетной панели районов добавлена явная кнопка `Скрыть`;
- кнопка видна только в compact layout и не меняет десктопную боковую панель;
- e2e smoke списка организаций теперь проверяет полный цикл: открыть фильтры, увидеть район, закрыть панель и сохранить отсутствие горизонтального overflow.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - сворачиваемые фильтры организаций

Продолжен инкремент roadmap 3.1 по списку организаций:

- на планшетах и телефонах панель районов больше не занимает первый экран по умолчанию;
- в сводке фильтров добавлена кнопка `Фильтры`, которая раскрывает и скрывает панель районов без изменения десктопного сценария;
- счетчик активных фильтров виден прямо в кнопке, поэтому состояние списка понятно даже при скрытой панели;
- класс панели вынесен в helper и покрыт unit-тестом, чтобы адаптивное поведение не сломалось при следующих правках.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - устойчивое контекстное меню организаций

Продолжен инкремент roadmap 3.1 по списку организаций:

- расчет позиции контекстного меню строки вынесен в общий helper без магических чисел в JSX;
- меню теперь учитывает ширину, высоту и отступ viewport, поэтому не уезжает за правый или нижний край;
- `Escape` закрывает контекстное меню явно, а остальные клавиши больше не прячут его случайно;
- добавлено CSS-ограничение ширины меню на узких экранах и unit-тесты расчета позиции.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - primary-действия в меню строк

Продолжен инкремент roadmap 2.4 по общему меню действий строк:

- `RowActionsMenu` получил явный флаг `primary` для основного действия строки;
- primary-пункт меню выделяется единым спокойным стилем, не меняя поведение secondary, danger, disabled и busy-действий;
- основные действия в сотрудниках, организациях, кампаниях, очереди рассылок и SMTP-профилях помечены как primary;
- добавлен unit-тест, который фиксирует стабильные классы primary/danger в меню.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - ARIA-семантика меню действий

Продолжен инкремент roadmap 2.4 по общему меню действий строк:

- `RowActionsMenu` теперь связывает кнопку открытия и выпадающее меню через `aria-controls`/`aria-labelledby`;
- для занятых действий добавлен явный признак `busy`, который блокирует пункт меню и выставляет `aria-busy`;
- существующие async-действия в кампаниях, очереди рассылок и SMTP-профилях получили busy-состояние без изменения бизнес-логики;
- добавлен unit-тест на ARIA-связку trigger/menu и недоступность busy-действия.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - устойчивое закрытие меню действий

Продолжен инкремент roadmap 2.4 по общему меню действий строк:

- `RowActionsMenu` закрывается при изменении viewport: resize и scroll больше не оставляют меню в старой позиции;
- глобальный Escape теперь не только закрывает меню, но и возвращает фокус на кнопку открытия;
- добавлены unit-тесты на закрытие по resize/scroll и возврат фокуса при глобальном Escape;
- поведение меню стало стабильнее для таблиц, мобильных карточек и длинных списков с прокруткой.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - адаптивное позиционирование меню действий

Продолжен инкремент roadmap 2.4 по общему меню действий строк:

- `RowActionsMenu` теперь выбирает горизонтальное открытие `start/end`, чтобы меню не уезжало за левый край на узких экранах;
- меню может открываться вверх, если под строкой недостаточно места до нижнего края viewport;
- открытие через ArrowUp фокусирует последний доступный пункт, а ArrowDown - первый доступный пункт;
- добавлены unit-тесты на ArrowUp-открытие и выбор позиции `start/up` при mocked viewport geometry;
- CSS меню действий получил явные классы `start`, `end`, `down` и `up`, что делает поведение предсказуемым для таблиц и мобильных карточек.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - keyboard-навигация меню действий

Продолжен инкремент roadmap 2.4 по общему меню действий строк:

- `RowActionsMenu` теперь переводит фокус на первый доступный пункт при открытии меню;
- добавлена клавиатурная навигация по доступным действиям через ArrowUp, ArrowDown, Home и End;
- Escape закрывает меню и возвращает фокус на кнопку открытия;
- disabled-действия исключаются из фокусной навигации, но остаются видимыми в списке;
- unit-тест `RowActionsMenu` теперь проверяет loading-label, disabled-поведение и клавиатурное перемещение между действиями.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - busy-состояния очереди рассылок

Продолжен инкремент roadmap по устойчивости действий в операционных таблицах:

- меню действий строки теперь поддерживает не только текст, но и loading-разметку, поэтому долгие действия могут показывать единый индикатор прямо в пункте меню;
- на странице очереди рассылок повтор сообщения показывает состояние "Возвращаем" для активной строки и блокирует повторный запуск этой операции;
- ручное обновление очереди в заголовке страницы блокируется на время загрузки и показывает состояние "Обновляем";
- retry/cancel после успешного действия принудительно обновляют данные, даже если обычный refresh уже был заблокирован как повторный пользовательский клик;
- добавлен unit-тест `RowActionsMenu`, который проверяет loading-label и disabled-поведение пункта меню.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - busy-состояния для опасных действий

Продолжен инкремент roadmap по устойчивости действий в рассылках и настройках:

- проверка SMTP-профиля теперь блокирует повторный запуск для проверяемой строки и кнопки в модальном окне;
- ручной запуск кампании получил отдельное busy-состояние и больше не допускает повторного клика во время проверки готовности и постановки в очередь;
- повтор проблемного сообщения в статистике кампании отделяет ошибку самого retry от ошибки последующего обновления статистики;
- обновление статистики кампании поддерживает управляемое пробрасывание ошибки для сложных сценариев, не меняя обычное поведение кнопки refresh;
- добавлен unit-тест, который проверяет блокировку retry-кнопки на время асинхронного повтора.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - устойчивые refresh-действия и загрузки

Продолжен инкремент roadmap по устойчивости CRM-экранов:

- список кампаний, очередь рассылок, SMTP-профили, задачи и справочник организаций теперь пробрасывают ошибки refresh наружу, поэтому `SearchPanel` больше не показывает успешное обновление при сбое API;
- первичные загрузки этих экранов очищают устаревшие строки и показывают понятный toast при ошибке;
- действия запуска, паузы, удаления кампаний, проверки SMTP и операций очереди рассылок получили отдельные сообщения для ошибки действия и ошибки последующего обновления списка;
- дашборд и редактор кампании теперь показывают toast при сбое загрузки, а ручной запуск кампании больше не оставляет необработанный rejected promise.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - единая обработка ошибок CRM-экранов

Продолжен инкремент roadmap по технической чистоте frontend и устойчивости UI:

- общий API-клиент больше не использует `any` при разборе ошибок и проверяет HTTP-статус через типобезопасный helper;
- списки и карточки организаций, сотрудников, задач и очереди рассылок переведены на общий `getApiErrorMessage`;
- ручные обновления, загрузка справочников задач, повтор и отмена сообщений очереди теперь показывают понятный toast при сбое;
- `SearchPanel` и загрузчик изображений сотрудника используют ту же нормализацию ошибок, что и API-формы;
- добавлены unit-тесты для чтения HTTP-статуса и отображения строковых ошибок refresh-действия.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - нормализация ошибок в формах рассылок

Продолжен инкремент roadmap по технической чистоте и устойчивости UI:

- страницы редактирования кампании и SMTP-профилей больше не читают `error.message` через `any` в `catch`;
- ошибки API нормализуются через общий `getApiErrorMessage`, поэтому network/timeout/backend-сообщения показываются единообразно;
- `getApiErrorMessage` теперь корректно обрабатывает строковые ошибки и покрыт отдельным unit-тестом;
- inline-ошибка предпросмотра расписания использует тот же нормализованный текст, что и toast.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - DB-пороги после IIS deploy

Продолжен инкремент roadmap 8.3 по проверке БД после деплоя:

- `check-mailing-db.ps1` получил read-only лимиты `-MaxFailedDispatchItems` и `-MaxQueueDepth`;
- результат DB-check теперь содержит `Issues` и `Limits`, чтобы причина падения была видна в JSON и в обычном выводе;
- GitHub Actions deploy умеет читать optional variables `DB_MAX_FAILED_DISPATCH_ITEMS`, `DB_MAX_QUEUE_DEPTH` и `DB_REQUIRE_TRANSPORT_PROFILE`;
- IIS-гайд дополнен настройкой строгой post-deploy проверки очереди, failed-сообщений и обязательного SMTP-профиля.

Проверки этого этапа включают parser-проверку PowerShell-скриптов, локальный DB-check с лимитами, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - quality gates перед IIS deploy

Продолжен инкремент roadmap 8.1 по надежности автодеплоя:

- GitHub Actions deploy теперь запускает backend-тесты до публикации API;
- перед deploy добавлен NuGet vulnerability scan для всей backend solution;
- frontend pipeline разделен на `npm ci`, `npm test`, `npm audit --audit-level=moderate` и `npm run build`;
- IIS-гайд обновлен: теперь явно описано, что workflow останавливается до backup и замены файлов, если тесты или проверки зависимостей падают.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB/E2E-проверку рассылки против SQL-БД.

## 2026-06-14 - проверка опубликованного config после IIS deploy

Продолжен инкремент roadmap 8.1 по smoke-проверкам после автодеплоя:

- `smoke-iis.ps1` теперь отдельно проверяет внешний production config и опубликованный `appsettings.Production.json` в папке API;
- GitHub Actions deploy передает в smoke путь `API_SITE_PATH\appsettings.Production.json`, чтобы деплой падал, если config не скопировался в publish-каталог;
- manual smoke checklist и IIS-гайд обновлены под актуальные параметры `smoke-iis.ps1` и `check-mailing-db.ps1`.

Проверки этого этапа включают parser-проверку PowerShell-скриптов, локальный IIS smoke против API/frontend, frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную DB-проверку против SQL-БД.

## 2026-06-13 - негативный SMTP-сценарий в smoke

Продолжен инкремент roadmap 4.9 по end-to-end проверке рассылки:

- smoke-скрипт теперь создает временный SMTP-профиль на неслушающем локальном порту и проверяет, что `test SMTP` возвращает ошибку;
- успешная отправка кампании запускается только после того, как негативный SMTP-сценарий действительно провалился ожидаемым образом;
- для диагностики в JSON-результат добавлен флаг `SmtpFailureChecked`;
- добавлен параметр `-SkipSmtpFailureCheck`, если в редком окружении нужно отключить негативную SMTP-проверку.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - проверка SMTP в smoke-рассылке

Продолжен инкремент roadmap 4.9 по end-to-end проверке рассылки:

- smoke-скрипт рассылки теперь выполняет `POST /api/transport-profiles/{id}/test` после создания временного SMTP-профиля;
- локальный SMTP catcher поднимается отдельно для проверки подключения и отдельно для реальной отправки письма;
- если проверка SMTP не проходит, smoke падает до создания кампании и явно показывает сообщение ошибки;
- cleanup SMTP catcher job вынесен в общий helper, чтобы не оставлять фоновые jobs после сбоя.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - детали попыток в статистике кампании

Продолжен инкремент roadmap 4.8 по статистике и очереди кампании:

- проблемные сообщения в статистике кампании теперь показывают число попыток, время ошибки, следующую попытку и SMTP-ответ;
- русское форматирование количества попыток вынесено в helper и покрыто unit-тестом;
- карточка проблемного сообщения получила отдельные стили для метаданных, чтобы текст и кнопки не наезжали друг на друга на узких экранах;
- компонентный тест панели статистики проверяет отображение диагностических метаданных.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - повтор проблемных сообщений кампании

Продолжен инкремент roadmap 4.8 по статистике и очереди кампании:

- в панели статистики кампании появилась кнопка "Повторить" для проблемных сообщений;
- действие доступно только для сообщений в статусах failed/deferred и использует общий API retry очереди;
- после повтора статистика кампании обновляется, а оператор получает toast о результате;
- добавлен компонентный тест, который проверяет вызов retry из списка проблемных сообщений.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - панель статистики кампании

Продолжен инкремент roadmap 4.8 по статистике и очереди кампании:

- блок статистики вынесен из большой формы `CampaignEditPage` в отдельный компонент `CampaignStatsPanel`;
- фильтр последних сообщений очереди, список проблемных сообщений, batch list и summary теперь тестируются отдельно от редактора кампании;
- страница кампании оставляет за собой только загрузку статистики и передачу ключей таблиц, что снижает риск регрессий в остальных вкладках;
- добавлены компонентные тесты пустого состояния, последней ошибки, списка пакетов и фильтрации failed-сообщений.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - часовой пояс расписания кампании

Продолжен инкремент roadmap 4.7 по расписанию кампаний:

- дефолтный часовой пояс рассылок синхронизирован между frontend и backend как `Asia/Novosibirsk`;
- ручное поле timezone заменено на select с поддерживаемыми часовыми поясами и описанием выбранного режима;
- старые нестандартные значения timezone остаются видимыми в форме, но inline validation просит выбрать поддерживаемый вариант;
- preview ближайших запусков показывает локальное время вместе с понятной подписью часового пояса и UTC-эквивалентом;
- добавлены frontend helper/component-тесты и backend-тест дефолтного часового пояса расписания.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - предпросмотр изображений во вложениях кампании

Продолжен инкремент roadmap 4.6 по вложениям редактора письма:

- для вложений с `image/*` появился авторизованный blob-предпросмотр прямо в карточке файла;
- предпросмотр загружается через API-клиент с Bearer-токеном, поэтому не зависит от публичного доступа к файлам;
- object URL очищаются при размонтировании/смене вложений, чтобы не оставлять лишние ресурсы в браузере;
- добавлены helper-тесты определения изображений и компонентные тесты `AttachmentManager` на загрузку превью и игнорирование не-image файлов.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - стартовый шаблон письма кампании

Продолжен инкремент roadmap 4.5 по редактору письма:

- в редакторе письма появилась кнопка "Вставить шаблон" для пустой HTML/text версии;
- шаблон заполняет HTML и plain text согласованным стартовым текстом, чтобы письмо не начиналось с пустого textarea;
- кнопка блокируется, если письмо уже заполнено, чтобы не затереть пользовательский текст;
- добавлены unit-тесты helper'а шаблона и Playwright smoke на вставку шаблона в редакторе кампании.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - источники получателей в preview кампании

Продолжен инкремент roadmap 4.4 по получателям рассылки:

- предпросмотр получателей теперь группирует найденные адреса по источникам;
- оператор видит, сколько адресов пришло из ручного списка, основного email организации, контактов и других источников;
- добавлен helper `buildRecipientSourceSummary` с unit-тестами для порядка, счётчиков и неизвестных кодов источника;
- Playwright smoke редактора кампании проверяет реальные коды источников preview и наличие summary в интерфейсе.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - сводка получателей кампании

Продолжен инкремент roadmap 4.4 по получателям рассылки:

- блок выбора организаций в кампании теперь показывает сводку выбранных получателей;
- оператор сразу видит число выбранных организаций, известных email, организаций с email, организаций без email и контактов;
- расчёт сводки вынесен в отдельный helper, чтобы не смешивать UI и бизнес-логику;
- добавлены unit-тесты расчёта и компонентный тест отображения сводки в `OrganizationPicker`.

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - быстрые фильтры рассылок

Продолжен инкремент roadmap 4.1 по списку рассылок:

- список рассылок получил быстрые кнопки фильтрации по статусам без обязательного нажатия "Применить";
- старый select статуса оставлен как дополнительный контрол для привычного сценария работы;
- responsive-разметка фильтров адаптирована под desktop, tablet и mobile без горизонтального overflow;
- Playwright smoke-тест проверяет, что быстрый фильтр отправляет в API корректный query-параметр `status` и очищает его при выборе "Все".

Проверки этого этапа включают frontend-тесты, Playwright smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - предупреждение о несохраненных изменениях организации

Продолжен инкремент roadmap 3.5 по уточнению сценариев карточки организации:

- при попытке вернуться к списку организаций с измененным draft теперь открывается модальное предупреждение;
- пользователь может остаться в карточке или выйти без сохранения с явным подтверждением;
- добавлен `beforeunload` guard для обновления/закрытия вкладки браузера при несохраненных изменениях;
- Playwright smoke карточки организации расширен сценарием изменения поля, отмены выхода и подтвержденного выхода к списку.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - layout карточки организации

Продолжен инкремент roadmap 3.4 по переработке layout карточки организации:

- вкладки карточки перенесены внутрь основной колонки, чтобы desktop-разметка работала как связка "форма 70% / summary 30%";
- sidebar организации на tablet/mobile поднимается над вкладками и больше не занимает боковую колонку рядом с формой;
- responsive CSS для карточки организации уплотнен на мобильной ширине без горизонтального overflow;
- добавлен Playwright smoke-тест карточки организации на desktop/tablet/mobile: проверяется 70/30 layout, порядок sidebar перед вкладками и отсутствие горизонтального overflow.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - центральная область карточки организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- центральная область вкладок вынесена в `components/organization/OrganizationRecordMain.tsx`;
- profile, support, relations и history теперь выбираются в controlled-компоненте, а workspace только передает активные вкладки и callbacks;
- пустые состояния для связей и истории до сохранения организации перенесены из workspace в новый компонент;
- добавлены компонентные тесты на рендер profile/support-вкладок и пустые состояния relations/history.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-13 - профиль организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- profile-вкладка вынесена в `components/organization/OrganizationProfileSection.tsx`;
- основные реквизиты, контакты, данные руководителя и legacy-заметки больше не собираются напрямую в основном workspace;
- `OrganizationRecordWorkspace` оставлен композиционным слоем для переключения вкладок и боковой панели;
- добавлены компонентные тесты на рендер profile-вкладки и проброс изменений формы в родительский draft-handler.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - секция поддержки организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- support-вкладка вынесена в `components/organization/OrganizationSupportSection.tsx`;
- вычисление статусов лицензий 1С/сайта и summary-карточек поддержки больше не находится в основном workspace;
- `OrganizationRecordWorkspace` больше не содержит JSX support-блоков и связанных helper'ов дат/лицензий;
- добавлены компонентные тесты на summary-карточки, статусы лицензий, все support-блоки и пустое состояние без сохранённых деталей.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - секция связей организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- overview-карточки и таблица связанных записей объединены в `components/organization/OrganizationRelationsSection.tsx`;
- построение preview-данных контактов, документов, договоров, реализаций, лицензий и заказов вынесено из основного workspace;
- `OrganizationRecordWorkspace` больше не содержит helper'ы форматирования сумм и preview-элементов связей;
- добавлены компонентные тесты на preview-карточки, активную таблицу, переключение вкладок и пустое состояние.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - история организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- вкладка истории вынесена в `components/organization/OrganizationHistoryWorkspace.tsx`;
- режим отображения событий и активный снимок 1С теперь управляются внутри компонента истории;
- основной workspace больше не содержит inline-таблицу событий, timeline/snapshot/audit ветвление и связанные импорты;
- добавлены компонентные тесты на переключение событий из ленты в таблицу, выбор снимка 1С, пустое состояние снимков и аудит.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - таблицы связей организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- вкладка связанных данных вынесена в `components/organization/OrganizationRelationsWorkspace.tsx`;
- таблицы контактов, документов, договоров, реализаций, лицензий и заказов больше не находятся inline в основном workspace;
- тип настроек колонок вынесен рядом с компонентом связей и переиспользуется родителем;
- добавлены компонентные тесты на выбор активной таблицы, пустое состояние, даты, суммы, boolean-статусы и статус передачи договора в 1С.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - поддержка 1С, сайта и программных блоков организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- блок `1С` вынесен в `components/organization/OrganizationOneCDetails.tsx`;
- блок `Сайт` вынесен в `components/organization/OrganizationSiteDetails.tsx`;
- блок `Блоки программ` вынесен в `components/organization/OrganizationProgramBlocks.tsx`;
- основной workspace больше не содержит JSX этих support-секций, их ссылочных helper'ов и вычисления programCards;
- добавлены компонентные тесты на заполненные данные, пустые состояния, boolean/zero-значения, статус лицензий и нестандартные программные блоки.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - зарплатный блок организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- блок `Зарплата` вынесен в `components/organization/OrganizationSalaryDetails.tsx`;
- основной workspace больше не содержит JSX зарплатных реквизитов, контактных ссылок и комментариев;
- добавлены компонентные тесты на заполненные данные, пустое состояние, boolean-значения и нулевые счетчики.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - банковские реквизиты организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- блок `ЭЦП и банковские реквизиты` вынесен в `components/organization/OrganizationBankDetails.tsx`;
- основной workspace больше не содержит JSX сетки банковских реквизитов и комментариев ЭЦП;
- добавлены компонентные тесты на заполненные реквизиты, пустое состояние и trim whitespace-only значений.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - legacy-заметки организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- блок `Legacy-заметки` вынесен в `components/organization/OrganizationLegacyNotes.tsx`;
- основной workspace больше не содержит JSX списка legacy-комментариев профиля и только передает детали организации;
- добавлены компонентные тесты на заполненные заметки, пустое состояние и trim whitespace-only значений.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - реквизиты руководителя организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- блок `Руководитель и служебные реквизиты` вынесен в `components/organization/OrganizationDirectorDetails.tsx`;
- основной workspace больше не содержит JSX карточек руководителя и только передает детали организации в профильную вкладку;
- добавлены компонентные тесты на заполненные реквизиты, fallback-значения, кликабельный телефон и email-ссылку.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - аудит карточки организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- блок аудита и сводных счетчиков карточки организации вынесен в `components/organization/OrganizationAuditSummary.tsx`;
- основной workspace больше не содержит JSX для audit-tab и только передает данные организации и число email-адресов;
- добавлены компонентные тесты на авторов аудита, счетчики связанных сущностей и пустое состояние без загруженных деталей.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - детали снимка 1С организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- сетка реквизитов выбранного снимка 1С вынесена в `components/organization/OrganizationSnapshotDetails.tsx`;
- пустое состояние выбранного снимка теперь живет рядом с компонентом деталей, а основной workspace только выбирает активный снимок;
- добавлены компонентные тесты на заполненный снимок, частичные данные, кликабельный телефон и пустое состояние.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - вкладки снимков 1С организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- переключатель снимков 1С вынесен в `components/organization/OrganizationSnapshotTabs.tsx`;
- для вкладок снимков добавлен отдельный UI-контракт `OrganizationSnapshotTabItem`;
- `OrganizationRecordWorkspace` больше не содержит JSX tab-кнопок снимков и только передает список, активный ключ и callback выбора;
- добавлены компонентные тесты на активное состояние, callback выбора снимка и пустой список вкладок.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - режим просмотра событий организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- переключатель режима событий `Лента / Таблица` вынесен в `components/organization/OrganizationEventViewModeTabs.tsx`;
- тип `OrganizationEventViewMode` переехал рядом с UI-компонентом режима просмотра;
- `OrganizationRecordWorkspace` больше не содержит JSX tab-кнопок режима событий и только передает активный режим;
- добавлены компонентные тесты на активное состояние и callback выбора режима просмотра событий.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - вкладки истории организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- переключатель `События / Снимки 1С / Аудит` вынесен в `components/organization/OrganizationHistoryTabs.tsx`;
- тип `OrganizationHistoryTab` переехал рядом с компонентом вкладок истории;
- `OrganizationRecordWorkspace` больше не содержит JSX верхнего переключателя истории и только передает активную вкладку;
- добавлены компонентные тесты на полный набор вкладок, активное состояние и callback выбора вкладки истории.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-12 - overview связанных данных организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- блок preview-карточек и tab-переключателя раздела `Связи` вынесен в `components/organization/OrganizationRelationsOverview.tsx`;
- тип `OrganizationRelationTab` переехал рядом с UI-контрактом связанных данных;
- `OrganizationRecordWorkspace` больше не содержит JSX сетки preview-карточек и inline tab-кнопок связанных записей;
- добавлены компонентные тесты на активное состояние, пустые preview-данные и callback выбора раздела через карточки и вкладки.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - status bar карточки организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- верхняя status bar карточки организации вынесена в `components/organization/OrganizationStatusBar.tsx`;
- бейджи видимости, признака менеджера, района, типа, статуса, флага и состояния сохранения теперь изолированы от основного workspace;
- `OrganizationRecordWorkspace` передает в status bar только готовые значения и больше не содержит JSX этого блока;
- добавлены компонентные тесты на полный набор бейджей, скрытие необязательных бейджей и dirty-состояние формы.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - summary сопровождения организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- summary-карточки раздела `Сопровождение` вынесены в `components/organization/OrganizationSupportSummary.tsx`;
- добавлен тип `OrganizationSupportSummaryItem` для UI-контракта карточек сопровождения;
- `OrganizationRecordWorkspace` оставляет расчет `supportCards`, но больше не содержит JSX summary-сетки;
- добавлены компонентные тесты на tone-классы, тексты карточек и пустой список summary.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - навигация вкладок карточки организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- основная навигация вкладок карточки организации вынесена в `components/organization/OrganizationViewTabs.tsx`;
- тип `OrganizationViewTab` переехал рядом с компонентом навигации;
- `OrganizationRecordWorkspace` больше не содержит JSX основных tab-кнопок и остается координатором активной вкладки;
- добавлены компонентные тесты на полный набор вкладок, активное состояние и callback выбора вкладки;
- стабилизирован `smoke-mailing-e2e.ps1`: временная кампания создается в статусе `Paused`, чтобы ручной smoke-run не конкурировал с автоматическим планировщиком.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - лента событий карточки организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- `EventTimeline` вынесен из workspace в компонент `components/organization/OrganizationEventTimeline.tsx`;
- форматирование дат, сумм, статусов и пустого состояния ленты событий теперь находится рядом с компонентом истории;
- `OrganizationRecordWorkspace` больше не содержит JSX ленты событий и использует новый компонент в режиме `Лента`;
- добавлены компонентные тесты на заполненную ленту, fallback-заголовки, метаданные, период, лицензионные чипы и empty state.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - sidebar карточки организации

Продолжен инкремент roadmap 3.3 по разделению `OrganizationRecordWorkspace`:

- блок `organization-record-sidebar` вынесен в отдельный компонент `components/organization/OrganizationSidebar.tsx`;
- паспорт организации, статусы, финансовые метрики, быстрые переходы, почтовые адреса, задачи и контекст карточки теперь изолированы от координатора вкладок;
- `OrganizationRecordWorkspace` передает в sidebar только данные, email-чипы и обработчики переходов к связанным данным/истории;
- добавлены компонентные тесты sidebar на паспортные поля, нормализацию ссылок `tel`/`https`, email-чипы, быстрые действия и пустые состояния.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - preview-карточка связей организации

Начат инкремент roadmap 3.3 по разделению большого `OrganizationRecordWorkspace`:

- `RelationPreviewCard` вынесен в отдельный компонент `components/organization/RelationPreviewCard.tsx`;
- тип `PreviewCardItem` переехал вместе с компонентом, чтобы workspace не хранил локальный UI-контракт;
- `OrganizationRecordWorkspace` остался координатором вкладок и использует новый компонент без изменения поведения карточек связей;
- добавлены компонентные тесты на активное состояние, формат счетчика, click-handler и пустое состояние preview-карточки.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - badges и мобильные поля OrganizationsTable

Закрыт инкремент roadmap 3.2 по читаемости таблицы организаций:

- колонка `Видимость` теперь отображает состояние организации через общий `StatusBadge`;
- колонка `Управленческая` также переведена на badge-формат для включаемого desktop-поля;
- для мобильной карточки организаций явно заданы labels для `ИНН`, района, типа, email-счетчика и количества контактов;
- служебные desktop-поля `Видимость` и `Управленческая` не попадают в мобильную карточку, даже если пользователь включил их в настройках таблицы;
- добавлен компонентный тест `OrganizationsTable` на читаемую мобильную карточку, счетчики, действия и badge-рендер скрытых колонок.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - режимы отображения DataTable

Закрыт инкремент roadmap 2.2 по mobile/tablet strategy для `DataTable`:

- добавлен явный `displayMode`: `auto`, `table`, `cards`;
- режим `auto` сохраняет текущее поведение: desktop показывает таблицу, мобильная ширина показывает карточки;
- режим `cards` больше не применяет пользовательские table-настройки видимости/ширины колонок к мобильной карточке;
- карточки строятся по `isPrimary`, `isActions`, `mobileLabel`, `mobileVisible` и `priority`, а настройки колонок остаются контрактом desktop-таблицы;
- добавлены компонентные тесты на принудительный `cards`/`table` режим и независимость карточек от сохраненной видимости колонок.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - compact pagination для мобильных экранов

Закрыт инкремент roadmap 7.4 по компактной пагинации:

- `Pagination` получил отдельный мобильный режим с кнопками `Назад`, `Вперед` и стабильным счетчиком `N из M`;
- desktop-режим с быстрым переходом на первую/последнюю страницу и номерами страниц сохранен без изменения поведения;
- на мобильной ширине длинный список номеров страниц больше не участвует в раскладке и не распирает страницу;
- добавлен компонентный тест на compact-навигацию и отсутствие page-number кнопок внутри compact-блока.

Проверки этого этапа включают frontend-тесты, Playwright responsive smoke-тесты, production-сборку, backend-тесты, backend-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - понятная ошибка недоступности API на входе

Закрыта задача `UX: улучшить Network Error на LoginPage`:

- добавлен общий formatter API-ошибок `getApiErrorMessage`;
- `api.ts` и `LoginPage` используют единые тексты для сетевой недоступности, timeout и сообщений backend;
- форма входа больше не показывает технический `Network Error`, если API недоступен или запрос оборвался;
- добавлены unit-тесты formatter и Playwright smoke-сценарий для сетевой ошибки логина.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - единые иконки действий

Закрыт следующий инкремент roadmap 7.3 по унификации action controls:

- добавлен общий компонент `ActionIcon` для поиска, сброса, обновления, возврата назад, меню действий, стрелок пагинации и раскрытия списка;
- `SearchPanel`, `RowActionsMenu`, `Pagination`, dashboard, карточки сотрудников и организаций переведены с локальных SVG на общий набор иконок;
- страницы больше не импортируют иконки из `SearchPanel` ради отдельных action-кнопок;
- добавлен unit-тест, который фиксирует декоративный SVG-контракт и поддержку всех вариантов иконок.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - responsive smoke рабочих экранов

Закрыт следующий инкремент roadmap 7.2 по проверке основных экранов на типовых ширинах:

- добавлен Playwright smoke-тест `workspace-responsive-smoke.spec.ts` для dashboard, сотрудников, организаций, рассылок, очереди рассылок и настроек;
- каждый экран проверяется на 360, 390, 768, 1024, 1366 и 1920 px без горизонтального overflow документа;
- тест использует мокированные API-ответы и авторизованную сессию, поэтому не зависит от текущих данных пользователя;
- исправлен планшетный layout очереди рассылок: сводные карточки и панель фильтров перестраиваются до того, как начинают распирать страницу.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - проверка Data Protection для SMTP-секретов

Закрыт следующий инкремент roadmap 5.5 по Data Protection и секретам:

- `GET /api/diagnostics/storage` теперь возвращает блок `Secrets` с результатом protect/unprotect round-trip через текущий Data Protection provider;
- общий статус диагностики становится `error`, если каталог ключей/загрузок доступен, но Data Protection не может корректно зашифровать и расшифровать probe-секрет;
- добавлен backend unit-тест, который защищает SMTP-пароль одним provider, пересоздает provider на той же папке ключей и проверяет расшифровку после условного restart/deploy.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - recovery очереди без полного чтения dispatch items

Продолжена работа над roadmap 5.4 и оптимизацией очереди рассылок:

- `RecoverStuckItemsAsync` больше не читает всю таблицу `MailDispatchItem`: зависшие `Processing` и просроченные queue reservations выбираются через `XPQuery` по статусам и временным порогам;
- `QueueDueItemsAsync` переносит фильтр статуса и due-date ближе к БД, сохраняя in-memory проверку `ChannelQueuedAtUtc` только для sentinel-значения `DateTime.MinValue`;
- добавлен backend unit-тест `DispatchServiceRecoveryTests` на возврат зависшего `Processing`, освобождение просроченной reservation и постановку due items в in-memory channel без повторной резервации свежих/будущих элементов.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную E2E-проверку рассылки против SQL-БД.

## 2026-06-11 - проверки удаления без полного чтения таблиц

Продолжена оптимизация backend-запросов из roadmap 5.2:

- проверки удаления SMTP-профиля, legacy-организации с задачами и legacy-организации, выбранной в целях кампаний, перенесены с `ToList().Any(...)` на query-level `XPQuery.Any(...)`;
- поведение защитных проверок сохранено: связанные SMTP-профили и организации по-прежнему нельзя удалить, пока они используются;
- добавлен backend unit-тест `DeletionGuardTests` для защитных сценариев удаления профиля и организации.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-11 - dashboard-сводка без лишнего чтения таблиц

Продолжена оптимизация backend-запросов из roadmap 5.2:

- счетчики организаций, активных кампаний, глубины очереди, отправленных и ошибочных сообщений за 24 часа в dashboard перенесены на `XPQuery.Count`;
- фильтр сотрудников по legacy-группе "Уволенные" оставлен с прежней case-insensitive логикой, чтобы не менять бизнес-результат;
- добавлен backend unit-тест `OverviewServiceTests` на сводные показатели dashboard из legacy и mailing-хранилищ.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-11 - статистика кампаний без полного чтения очереди

Продолжена оптимизация backend-запросов рассылок:

- статистика `/api/campaigns/{id}/stats` больше не выгружает все партии и элементы очереди перед фильтром кампании;
- фильтр `campaignId` для `MailDispatchBatch` и `MailDispatchItem` перенесен на `XPQuery`, а существующая логика счетчиков и последних записей сохранена;
- добавлены backend unit-тесты `StatisticsServiceTests`, которые проверяют, что статистика, failed/deferred списки и последние элементы не подмешивают данные другой кампании.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальный E2E smoke против API и SQL-БД.

## 2026-06-11 - фильтры списка кампаний ближе к БД

Продолжена оптимизация списков из roadmap 5.2:

- список `/api/campaigns` больше не выгружает все кампании перед обычной фильтрацией и пагинацией;
- фильтр статуса, `totalCount`, сортировка и `Skip/Take` для списка кампаний перенесены на `XPQuery`;
- текстовый поиск оставлен in-memory, чтобы сохранить case-insensitive поиск по названию, теме, cron и имени SMTP-профиля без риска SQL-трансляции;
- добавлены backend unit-тесты `CampaignServiceTests` на статусный фильтр, paging, `totalCount` и поиск по SMTP-профилю.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-11 - фильтры диагностики очереди ближе к БД

Продолжена работа по снижению риска `ToList()` до фильтрации и пагинации:

- `/api/dispatch/items` теперь применяет простые фильтры по статусу, кампании и партии на `XPQuery` до материализации списка;
- `/api/dispatch/batches` применяет фильтр кампании, сортировку, `Skip/Take` и подсчет `totalCount` на запросе к хранилищу;
- текстовый поиск элементов очереди оставлен in-memory, чтобы сохранить case-insensitive поиск по нескольким nullable-полям без риска сломать SQL-трансляцию;
- добавлены backend unit-тесты `DispatchDiagnosticsServiceTests` на фильтрацию, paging и корректный `totalCount`.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-11 - ручной smoke-чеклист после deploy

Добавлен `docs/manual-smoke-test.md` - единый ручной checklist для проверки PulsCRM после локального запуска, IIS deploy или rollback:

- зафиксированы фактические URL frontend/API, SQL Server, БД, commit и итог проверки;
- описаны шаги для healthcheck, логина, dashboard, сотрудников, организаций, SMTP-профилей, кампаний, очереди рассылок и адаптивности;
- добавлены команды для `smoke-iis.ps1`, `check-mailing-db.ps1` и `smoke-mailing-e2e.ps1`;
- отдельный блок проверяет desktop/tablet/mobile ширины и типовые проблемы верстки: горизонтальный скролл, наезд текста, меню действий и пагинацию;
- Playwright smoke больше не переиспользует случайный локальный сервер на тестовом порту и запускает собственный Vite с `--strictPort`;
- `docs/README.md` дополнен ссылкой на новый checklist.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, parser-проверку PowerShell-скриптов, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальный E2E smoke против API и SQL-БД.

## 2026-06-11 - E2E smoke реальной отправки рассылки

Добавлен проверяемый сценарий ручной end-to-end приемки рассылок:

- новый `scripts/smoke-mailing-e2e.ps1` поднимает локальный SMTP-catcher на loopback-порту;
- скрипт создает временный SMTP-профиль, временную кампанию с ручным получателем и запускает manual run через API;
- проверяется readiness кампании, создание dispatch batch, получение письма SMTP-catcher'ом и статистика кампании `sent >= 1`;
- после успешной проверки временная кампания, SMTP-профиль и captured `.eml` удаляются, чтобы локальная тестовая БД не засорялась;
- для разбора неуспешного сценария добавлен `-KeepArtifacts`;
- IIS-гайд дополнен командами запуска E2E smoke локально через dev JWT и на сервере через логин/пароль.

Проверки этого этапа включают parser-проверку PowerShell-скрипта, локальный E2E smoke против API и SQL-БД, backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan и проверку кодировки.

## 2026-06-10 - проверка БД после IIS deploy

Добавлен read-only SQL-check для проверки состояния модуля рассылок после деплоя:

- новый `scripts/check-mailing-db.ps1` читает `MailingDb` из `appsettings.Production.json` или принимает строку подключения напрямую;
- XPO-префикс `XpoProvider=...` автоматически убирается перед подключением через SQL Server provider;
- проверяется наличие ожидаемых таблиц `Mail*`, количество строк в каждой таблице, SMTP-профили, глубина очереди и failed-сообщения;
- workflow `Deploy Production` запускает DB-check после API/frontend smoke;
- IIS-гайд дополнен ручным запуском DB-check и опцией `-RequireTransportProfile` для строгой приемки SMTP-профиля.

Проверки этого этапа включают parser-проверку PowerShell-скриптов, локальный запуск DB-check против SQL-БД, backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - backup и rollback IIS deploy

Деплой на IIS получил безопасный путь отката:

- `scripts/deploy-iis.ps1` перед `robocopy /MIR` сохраняет текущие публикации API и frontend в backup-папку;
- backup хранится в формате `yyyyMMdd-HHmmss` с подпапками `Api`, `Web` и `manifest.json`;
- workflow `Deploy Production` использует `DEPLOY_BACKUP_PATH` или значение по умолчанию `C:\Apps\PulsCRM\Backups`;
- добавлен `scripts/rollback-iis.ps1` для ручного восстановления API/Web из выбранного backup;
- backup retention ограничивает хранение пятью последними публикациями;
- IIS-гайд дополнен командами поиска backup-папок и ручного rollback.

Проверки этого этапа включают parser-проверку PowerShell-скриптов, локальный dry-run backup/rollback на временных каталогах, backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - smoke-проверка IIS deploy

Добавлена отдельная post-deploy проверка для IIS-публикации:

- новый `scripts/smoke-iis.ps1` проверяет API `/health`, frontend root, frontend bundle и опционально публичный `/api/auth/users?take=1`;
- workflow `Deploy Production` запускает smoke после копирования API/frontend и использует `FRONTEND_URL` для warmup;
- smoke выводит фактические `PRODUCTION_API_URL`, `HEALTHCHECK_URL`, `FRONTEND_URL` и проверяет наличие production-конфига API;
- IIS-гайд дополнен переменной `FRONTEND_URL` и примером ручного запуска smoke-скрипта;
- скрипт локально проверен против API на SQL-БД и frontend dev-сервера.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - e2e smoke формы кампании

Добавлен браузерный smoke-тест для ключевого сценария подготовки рассылки в форме кампании:

- новая кампания открывается с авторизованной сессией и доступным SMTP-профилем;
- заполняются название, тема, SMTP-профиль, HTML/plain text письмо и ручной получатель;
- проверяется API-предпросмотр получателей и отображение найденного email;
- проверяется readiness-блок кампании с положительным результатом готовности;
- проверяется предпросмотр расписания и отсутствие горизонтального overflow страницы;
- общий мок SMTP-профиля вынесен в e2e helpers и переиспользуется в тестах настроек.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - e2e smoke операционных разделов

Расширено браузерное покрытие рабочих разделов, которые критичны для сценария рассылок и эксплуатации:

- добавлен Playwright smoke для списка рассылок `/campaigns` с моками кампании, темы и SMTP-профиля;
- добавлен smoke для `/dispatch`, который проверяет сообщения очереди и переключение на вкладку партий;
- добавлен mobile smoke для `/settings` со списком SMTP-профилей в карточном режиме;
- для SMTP-карточки проверяется отсутствие горизонтального overflow на viewport 390px;
- покрытие Playwright выросло до 11 smoke-сценариев.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - e2e smoke авторизованных списков

Расширены Playwright smoke-тесты для проверки не только страницы входа, но и рабочих CRM-экранов после авторизации:

- добавлены общие e2e helpers для моков авторизации, JSON-ответов и проверки горизонтального переполнения страницы;
- проверяется открытие `/employees` с авторизованной сессией, оболочкой приложения и данными сотрудника;
- проверяется открытие `/organizations` на мобильном viewport с карточным режимом таблицы;
- проверяется, что мобильный список организаций не создает горизонтальный overflow страницы;
- Playwright-артефакты `test-results`, `playwright-report` и `blob-report` исключены из git.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - Playwright smoke-тесты

Добавлен браузерный smoke-слой для проверки критичных фронтенд-сценариев до ручной приемки:

- добавлен `@playwright/test` и npm-скрипт `test:e2e`;
- добавлена проверка страницы входа, ручного ввода логина и пароля;
- защищенный маршрут `/employees` проверяется на редирект неавторизованного пользователя к `/login`;
- ошибка авторизации из API показывается пользователю в форме входа;
- форма входа проверяется на mobile, tablet и desktop viewport без горизонтального переполнения;
- сетевые ответы мокируются в браузере, поэтому smoke-тесты не требуют поднятого API и не меняют локальную SQL-БД.

Проверки этого этапа включают backend-тесты, frontend-тесты, Playwright smoke-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - тесты настроек таблиц

Расширено frontend-покрытие `DataTable`, чтобы настройки рабочих таблиц не ломались при дальнейшей полировке интерфейса:

- добавлены DOM-тесты открытия панели настроек колонок;
- проверяется загрузка сохраненной видимости колонок из `localStorage`;
- проверяется защита от состояния, где все колонки скрыты;
- покрыто сохранение draft-настроек только после подтверждения;
- покрыт сброс настроек к значениям по умолчанию.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - компонентные frontend-тесты

Расширено frontend-покрытие критичных рабочих контролов CRM:

- добавлен `jsdom` для DOM-тестов Vitest без запуска браузера;
- добавлены компонентные тесты `Pagination` для перехода по страницам и выбора количества строк;
- добавлены компонентные тесты `SearchPanel` для debounce-поиска, сброса и запуска поиска по Enter/кнопке;
- тесты проверяют реальные React-события через `react-dom` render и synthetic events;
- покрытие фронтенда выросло до 10 test files и 35 tests.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - unit-тесты выбора получателей

Расширено backend-покрытие логики формирования аудитории рассылки:

- добавлены unit-тесты `RecipientResolver` для ручного списка email;
- проверяется разбор email через `;`, перенос строки и табуляцию;
- невалидные адреса отбрасываются до постановки в очередь;
- дубликаты email внутри одной организации или ручного списка не попадают в результат повторно;
- покрыт выбор источников организации: основной email, руководитель, зарплата, контакты;
- тесты используют in-memory XPO UnitOfWork и не обращаются к реальной SQL-БД.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - unit-тесты сборки писем

Расширено backend-покрытие сценариев рассылки перед реальной SMTP-отправкой:

- добавлены unit-тесты `MailComposer` для HTML-письма и автоматической plain text версии;
- проверяется формирование отправителя, получателя, reply-to, темы и message id;
- покрыты обычные файловые вложения из `IFileStorageService`;
- покрыты inline-вложения с `Content-ID` для `cid:` изображений;
- XPO-объекты в тестах создаются в изолированной in-memory session, чтобы не загрязнять `Session.DefaultSession`.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - диагностика Data Protection и storage

Добавлена защищенная backend-диагностика серверных каталогов, чтобы после деплоя быстро проверять права API на ключи и файлы:

- появился маршрут `GET /api/diagnostics/storage` под Bearer JWT;
- диагностика проверяет каталоги Data Protection keys и uploads через create/write/read/delete probe-файл;
- публичный `/health` не раскрывает абсолютные пути сервера;
- общий helper путей используется и для диагностики, и для конфигурации Data Protection;
- добавлены unit-тесты на нормализацию путей и проверку записи;
- документация описывает, что потеря ключей Data Protection может сделать сохраненные SMTP-пароли нечитаемыми.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - hardening recovery очереди рассылок

Усилена устойчивость очереди рассылок при рестартах IIS/API и повторной работе scheduler:

- правила retry delay, recovery processing и сброса queue reservation вынесены в тестируемый `DispatchRecoveryPolicy`;
- таймауты `ProcessingTimeoutMinutes` и `QueueReservationTimeoutMinutes` добавлены в конфигурацию `Dispatch`;
- recovery больше не держит магические значения таймаутов внутри сервиса;
- scheduled scheduler перед созданием партии проверяет, нет ли уже партии для той же кампании и того же времени запуска;
- ключ `DispatchKey` формируется через единый helper с нормализацией email;
- добавлены backend unit-тесты для retry backoff, recovery thresholds, queue reservation и scheduled batch guard.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - интерфейс диагностики очереди рассылок

Добавлена операторская страница `/dispatch` для диагностики очереди без прямого доступа к SQL:

- в боковое меню добавлен раздел "Очередь";
- страница показывает сообщения очереди и партии отправки отдельными вкладками;
- добавлены фильтры по статусу, ID кампании, ID партии и поиску по email/организации/ошибке/SMTP-ответу;
- для failed/deferred сообщений доступна повторная постановка в очередь, для неотправленных сообщений - отмена;
- таблицы получили настройки колонок, пагинацию и мобильное карточное отображение;
- добавлены frontend unit-тесты для правил статусов, retry/cancel и построения query.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки, browser smoke и локальную проверку API против SQL-БД.

## 2026-06-10 - админская диагностика очереди рассылок

Добавлен backend API для диагностики очереди рассылок без прямого доступа к SQL:

- появился защищенный контроллер `/api/dispatch`;
- добавлены списки элементов очереди и партий отправки с фильтрами и пагинацией;
- для элементов очереди добавлены операции `retry` и `cancel`;
- frontend API-клиент получил типы и функции для новых диагностических маршрутов;
- обновлена документация `docs/backend-api.md`.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки и локальную проверку API против SQL-БД.

## 2026-06-10 - улучшение статистики очереди рассылок

Продолжена доработка вкладки "Статистика" в карточке кампании:

- stats API теперь отдельно возвращает последние failed и deferred сообщения;
- во вкладку статистики добавлена кнопка ручного обновления;
- добавлена сводка последних проблемных сообщений с текстом ошибки или SMTP-ответом;
- таблица последних сообщений получила фильтры по статусам очереди;
- в таблицу добавлены время постановки в очередь, следующая попытка и SMTP-ответ/message id.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки, browser smoke и локальную проверку API против SQL-БД.

## 2026-06-10 - валидация письма и inline-вложений

Продолжена доработка редактора письма в кампаниях:

- добавлена диагностика HTML/текстовой версии письма прямо во вкладке "Письмо";
- проверяется пустое письмо, отсутствие текстовой версии и слишком короткий plain text;
- проверяются встроенные изображения: пустой `Content-ID`, неиспользованный `cid:` и ссылка на `cid:` без matching inline-файла;
- readiness API теперь тоже учитывает ошибки встроенных изображений и предупреждает о неиспользованных inline-файлах;
- менеджер вложений получил pre-upload проверку размера, понятное отображение `cid:...` и кнопку копирования токена.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки, browser smoke и локальную проверку API против SQL-БД.

## 2026-06-10 - улучшение конструктора расписания кампаний

Продолжена доработка сценария подготовки рассылки:

- часовой пояс новой кампании изменен на `Asia/Novosibirsk`, чтобы дефолт соответствовал локальному окружению проекта;
- выбор типа расписания переведен с выпадающего списка на сегментированные вкладки с кратким описанием режима;
- добавлена inline-валидация расписания: некорректный старт, конец раньше старта, неверные интервалы и базовая форма cron;
- предпросмотр ближайших запусков получил loading state и inline-ошибку рядом с формой;
- логика валидации расписания вынесена в unit-тестируемый helper.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, проверку кодировки, browser smoke и локальную проверку API против SQL-БД.

## 2026-06-10 - вкладки и dirty-state карточки кампании

Продолжена реализация roadmap по разделу рассылок:

- карточка кампании разделена на вкладки "Основное", "Получатели", "Письмо", "Расписание", "Проверка и запуск", "Статистика";
- добавлен индикатор несохраненных изменений и стандартное предупреждение браузера при закрытии страницы с измененным черновиком;
- кнопки проверки получателей и готовности теперь автоматически открывают соответствующую вкладку с результатом;
- логика сравнения черновика кампании вынесена в тестируемый helper с нормализацией выбранных организаций и сохранением порядка вложений;
- вкладки адаптированы для узких экранов через горизонтальную прокрутку без поломки ширины формы.

Проверки этого этапа включают frontend-тесты, production-сборку, проверку кодировки, browser smoke на desktop/mobile и локальную проверку API против SQL-БД.

## 2026-06-10 - улучшение CRM-интерфейса и контроль рассылок

В этом пакете исправлений начата практическая реализация roadmap по доведению разделов CRM до стабильного состояния:

- добавлены мобильные карточки для таблиц, чтобы данные организаций, сотрудников, кампаний и SMTP-профилей читались на телефонах и планшетах без горизонтального скролла;
- унифицированы действия в строках таблиц через компактное меню, чтобы кнопки не ломали верстку и не наезжали на текст;
- добавлены общие бейджи статусов для кампаний и транспортных профилей;
- исправлено отображение ошибки входа при недоступном API: вместо общего `Network Error` пользователь получает понятное сообщение;
- удаление кампаний и SMTP-профилей переведено с браузерного `confirm` на единый модальный диалог приложения;
- добавлены модульные тесты расчета расписаний рассылок, чтобы фиксировать поведение одноразовых, интервальных и cron-запусков;
- сохранены подробные документы анализа и roadmap для следующих этапов доработки.

Следующие крупные блоки остаются в roadmap: полноценная проверка сценариев рассылок, доработка фильтров и пагинации во всех таблицах, визуальное выравнивание форм редактирования, расширение E2E-проверок и проверка доступности интерфейса.

## 2026-06-10 - второй проход по спискам, фильтрам и frontend-тестам

Продолжена стабилизация интерфейса списков:

- раздел "Работа" получил сброс фильтров, статусные бейджи и корректный порядок полей в мобильных карточках;
- справочник выбора организаций в кампании переведен на общие настройки page size и получил мобильное действие "Выбрать/Выбрано";
- таблицы предпросмотра получателей, последних пакетов и последних сообщений в карточке кампании получили приоритеты мобильного отображения;
- статусы последних сообщений рассылки теперь отображаются бейджами, а не простым текстом;
- добавлен `npm test` на Vitest и unit-тесты для нормализации и загрузки размера страницы из `localStorage`.
- обновлены frontend-зависимости `axios`, `react-router-dom`, `vite`, `@vitejs/plugin-react` и `vitest`; `npm audit` теперь не показывает уязвимостей.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, browser smoke на desktop/mobile и локальную проверку API против SQL-БД.

## 2026-06-10 - третий проход по организациям и backend-зависимостям

Продолжена работа по roadmap для раздела организаций и технической чистоты backend:

- обновлен `MailKit` до версии `4.17.0`, после чего `dotnet list package --vulnerable` больше не показывает уязвимых NuGet-пакетов;
- добавлена явная проверка SMTP-сервера перед подключением в отправке писем и тесте SMTP-профиля, чтобы новые nullable-требования MailKit не оставляли предупреждений сборки;
- фильтры районов организаций получили верхнюю summary-панель над таблицей: видно активный районный фильтр, поисковую строку, количество найденных организаций и кнопку сброса;
- логика формирования summary выбранных районов вынесена в тестируемый helper;
- добавлены unit-тесты для выбора района без значения и для текста summary выбранных районов.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, browser smoke и локальную проверку API против SQL-БД.

## 2026-06-10 - readiness checklist кампаний

Добавлена диагностика готовности кампании перед запуском:

- появился API `POST /api/campaigns/readiness`, который проверяет название, тему, тело письма, SMTP-профиль, источники получателей, фактическое количество email и расписание;
- ручной запуск кампании в UI теперь сначала выполняет readiness-проверку и не ставит кампанию в очередь, если есть блокирующие ошибки;
- карточка кампании получила блок "Готовность к запуску" со сводкой, количеством организаций/получателей и списком пунктов проверки;
- предупреждение о SMTP-профиле по умолчанию не блокирует запуск, но явно показывается оператору;
- добавлены frontend unit-тесты для отображения summary и статусов readiness checklist.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, browser smoke и локальную проверку API против SQL-БД.

## 2026-06-10 - фильтр организаций с email для рассылок

Продолжена доработка сценария подготовки кампании к реальной отправке:

- API списка организаций получил параметр `hasEmail`, который фильтрует организации с email до пагинации и корректно считает `totalCount`;
- API районов организаций учитывает `hasEmail`, чтобы счетчики районов в справочнике получателей соответствовали активному фильтру;
- справочник выбора организаций в кампании получил включенный по умолчанию фильтр "Только организации с email";
- в таблицу выбора организаций добавлена колонка количества email;
- summary справочника показывает, сколько организаций найдено с учетом email-фильтра;
- добавлен unit-тест для текста summary email-фильтра.

Проверки этого этапа включают backend-тесты, frontend-тесты, production-сборку, `npm audit`, NuGet vulnerability scan, browser smoke и локальную проверку API против SQL-БД.
